{
  "name": "sociologor",
  "private": true,
  "version": "1.7.0",
  "type": "module",
  "description": "Sociologor — 15 fiches de sociologie, leurs concepts et leurs filiations, disponibles hors connexion.",
  "scripts": {
    "dev": "vite",
    "build": "npm run doc:audit && vite build && cp dist/index.html dist/404.html",
    "preview": "vite preview",
    "doc:audit": "node scripts/doc-audit.mjs",
    "test": "playwright test",
    "test:desktop": "playwright test --project=desktop",
    "verify": "npm run doc:audit && npm run build && npm test"
  },
  "dependencies": {
    "marked": "^12.0.2",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.26.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.8",
    "vite-plugin-pwa": "^0.20.5"
  }
}
